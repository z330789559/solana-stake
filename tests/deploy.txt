pretty audit load shuffle couch bring drop soap mean movie market elevator
unknown junior wait  tribe hole gym put nephew abandon someone shop unable

solana-keygen recover 'prompt://?key=0/0' -o ./tests/deploy.json --force
